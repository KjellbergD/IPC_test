project('pipeline', 'c')

sender = executable('sender', files('src/ipc_sender.c'), 
            install : true)

reviever = executable('receiver', files('src/ipc_receiver.c'), 
            install : true)